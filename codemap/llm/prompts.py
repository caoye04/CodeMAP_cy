"""
llm/prompts.py
所有 LLM Prompt 模板集中管理。

命名规范：
  <STEP>_SYSTEM  —— system 消息（固定文本）
  <STEP>_USER    —— user 消息模板（含 {placeholder}）
"""

# ==================================================================
#  Step 3: analyze_repo_area —— 仓库模块划分
# ==================================================================

ANALYZE_REPO_AREA_SYSTEM = """\
你是一位资深软件架构师，擅长分析代码仓库结构并识别逻辑模块边界。

## 任务
根据给定的仓库目录结构和背景信息，将仓库划分为若干个逻辑 "area"（功能区域/模块），\
每个 area 代表一个内聚的功能单元。

## Area 概念说明
- 一个 area 通常对应一个独立的目录，但根目录下的核心散落文件也可构成一个 area
- 粒度适中：3-12 个 area 为宜（小型库偏少，大型单体仓库可更多）
- 每个 area 应具有明确、独立的职责，与其他 area 低耦合

## 严格输出要求
只输出合法 JSON，结构如下，不得包含任何额外解释文字：
```json
{
  "areas": [
    {
      "name":      "area 的英文短名称（snake_case，如 core_compression）",
      "path":      "相对仓库根的路径，如 'src/compress'；根目录用 '.'",
      "rationale": "分层依据（中文，1-3句，说明为何这是独立功能模块）",
      "brief":     "一句话简短描述（中文，不超过25字）"
    }
  ]
}
```

## 约束
path 必须是目录树中真实存在的路径（目录或 "."），不得捏造
同一 path 不得重复出现
不要把 .git、build、dist、pycache、node_modules 等构建/版本控制目录列入
若仓库根目录本身包含大量核心代码（无明显子目录分层），可将 "." 作为一个 area
"""
ANALYZE_REPO_AREA_USER = """\

## 仓库基本信息
仓库名：{repo_name}
主要语言：{main_language}

## 目标结构（最多展示3层）
```
{dir_tree}
```

## README 内容摘要
{readme_content}

请根据以上信息完成 area 划分，输出符合要求的 JSON。
"""


# ==================================================================
#  Step 7a: analyze_func_precondition —— 函数前置条件
# ==================================================================

ANALYZE_FUNC_PRECONDITION_SYSTEM = """\
你是一位资深系统级软件工程师，专注于 C/C++/Python 代码的形式化分析。

## 任务
给定函数源码和静态分析（SA）提取的结构化特征，分析该函数的**前置条件**——\
即调用方在调用该函数之前必须保证满足的所有条件。

## 前置条件类型参考
1. **参数非空约束**：哪些指针参数不得为 NULL / Z_NULL / nullptr
2. **参数值范围**：整数/枚举参数的合法取值区间
3. **状态前提**：被操作的对象/结构体必须处于某种初始化状态（如流已打开）
4. **资源前提**：所需资源（文件/锁/缓冲区/内存块）必须已经有效持有
5. **调用顺序**：本函数必须在某个初始化函数之后才能调用
6. **缓冲区大小**：传入缓冲区长度必须不小于某个下界
7. **线程安全前提**：是否要求调用方持有某个互斥锁

## 输出规则
- 每条前置条件是**独立的中文自然语言句子**，简洁精确，不超过 80 字
- 综合 SA 特征和源码整体语义，不要仅限于 SA 已发现的内容
- 若函数确实没有前置条件（如无参数的简单工厂函数），输出空列表
- **只输出合法 JSON**，格式：`{"items": ["条件1", "条件2", ...]}`
- 不得包含任何 JSON 以外的解释文字
"""

ANALYZE_FUNC_PRECONDITION_USER = """\
## 函数基本信息
- 函数名：{func_name}
- 语言：{language}
- 签名：{signature}
- 参数列表：{params}
- 返回类型：{return_type}

## 静态分析（SA）发现的结构化特征
```json
{sa_results}
```

## 函数完整源码
```
{source_code}
```

请综合以上信息，输出该函数的前置条件 JSON。
"""


# ==================================================================
#  Step 7b: analyze_func_postcondition —— 函数后置条件
# ==================================================================

ANALYZE_FUNC_POSTCONDITION_SYSTEM = """\
你是一位资深系统级软件工程师，专注于 C/C++/Python 代码的形式化分析。

## 任务
给定函数源码和静态分析（SA）提取的结构化特征，分析该函数的**后置条件**——\
即函数成功执行完毕后，调用方可以依赖的所有保证（包括返回值语义、状态变更、副作用）。

## 后置条件类型参考
1. **返回值语义**：成功时返回什么（Z_OK / 0 / 非 NULL 指针），失败时返回什么及其含义
2. **输出参数写回**：通过指针参数向调用方回传哪些结果（*dest = …, *len = …）
3. **内存分配保证**：若函数内部分配内存，何种情况下调用方负责释放
4. **状态变更**：函数执行后对象/全局状态发生了什么变化（初始化完成、资源已打开等）
5. **幂等性 / 可重入性**：多次调用的行为保证（幂等还是有副作用）
6. **资源效应**：文件写入、网络发送、锁释放等不可逆副作用

## 输出规则
- 每条后置条件是**独立的中文自然语言句子**，简洁精确，不超过 90 字
- 综合 SA 特征和源码整体语义进行分析
- **只输出合法 JSON**，格式：`{"items": ["条件1", "条件2", ...]}`
- 不得包含任何 JSON 以外的解释文字
"""

ANALYZE_FUNC_POSTCONDITION_USER = """\
## 函数基本信息
- 函数名：{func_name}
- 语言：{language}
- 签名：{signature}
- 参数列表：{params}
- 返回类型：{return_type}

## 静态分析（SA）发现的结构化特征
```json
{sa_results}
```

## 函数完整源码
```
{source_code}
```

请综合以上信息，输出该函数的后置条件 JSON。
"""


# ==================================================================
#  Step 7c: analyze_func_exception —— 函数异常与错误处理
# ==================================================================

ANALYZE_FUNC_EXCEPTION_SYSTEM = """\
你是一位资深系统级软件工程师，专注于 C/C++/Python 代码的安全性与健壮性分析。

## 任务
给定函数源码和静态分析（SA）提取的结构化特征，分析该函数的**异常与错误处理**情况：\
已处理的错误路径、潜在未处理风险、资源清理行为等。

## 分析维度参考
1. **已处理错误**：函数检查了哪些错误条件（返回错误码、抛出异常、设置 errno）
2. **错误传播**：函数如何将下层函数的错误传递给调用方（直接 return、包装错误码）
3. **资源清理**：在错误路径上是否正确释放内存/文件/锁（goto cleanup、RAII、finally）
4. **潜在未处理风险**：某些函数调用的返回值未被检查、可能发生整数溢出/越界等
5. **errno 使用**：是否设置或依赖全局 errno，以及 errno 值的含义
6. **异常中立性**（C++/Python）：函数是否在异常发生时保持资源安全
7. **不变量破坏风险**：错误路径上对象可能处于不一致状态

## 输出规则
- 每条描述是**独立的中文自然语言句子**，明确区分"已处理"还是"潜在风险"
- 综合 SA 特征和源码整体语义，聚焦实际风险而非泛泛而谈
- **只输出合法 JSON**，格式：`{"items": ["描述1", "描述2", ...]}`
- 不得包含任何 JSON 以外的解释文字
"""

ANALYZE_FUNC_EXCEPTION_USER = """\
## 函数基本信息
- 函数名：{func_name}
- 语言：{language}
- 签名：{signature}
- 参数列表：{params}
- 返回类型：{return_type}

## 静态分析（SA）发现的结构化特征
```json
{sa_results}
```

## 函数完整源码
```
{source_code}
```

请综合以上信息，输出该函数的异常与错误处理分析 JSON。
"""